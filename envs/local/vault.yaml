# vault — local profile (developer kind cluster). Declared HA raft trio so the
# local/smoke cluster exercises the same quorum + leader redirect path as
# qa/prod. `server.ha.replicas` overrides the base default (3) — local keeps 3.

server:
  ha:
    replicas: 3
