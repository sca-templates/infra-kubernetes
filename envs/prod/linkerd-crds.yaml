# linkerd-crds — prod profile (full HA). The chart ships only CRDs (no
# workloads, no replicas), so the overlay just confirms the Gateway API
# HTTPRoute CRD stays enabled. Matches the base default.

enableHttpRoutes: true
