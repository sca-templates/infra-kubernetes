# vault — prod profile (full HA: 3 replicas, real storage).

replicaCount: 3

server:
  dataStorage:
    size: 20Gi
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi
